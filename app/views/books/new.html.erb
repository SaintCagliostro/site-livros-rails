<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <title>Novo Livro</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <style>
      :root {
          --primary-dark: #1a1a2e;
          --primary-blue: #16213e;
          --accent: #0f3460;
          --light-bg: rgba(255, 255, 255, 0.92);
          --glass-bg: rgba(255, 255, 255, 0.88);
          --border-light: rgba(0, 0, 0, 0.06);
          --text-primary: #1a202c;
          --text-secondary: #4a5568;
          --text-muted: #a0aec0;
      }

      * {
          box-sizing: border-box;
      }

      body {
          background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary-blue) 50%, var(--accent) 100%);
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
          color: var(--text-primary);
          line-height: 1.6;
          min-height: 100vh;
          padding: 2rem 0;
          margin: 0;
      }

      .form-container {
          max-width: 800px;
          margin: 0 auto;
          padding: 0 1.5rem;
      }

      .form-card {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 3.5rem;
          box-shadow: 0 25px 70px rgba(0,0,0,0.15);
          overflow: hidden;
          position: relative;
      }

      .form-header {
          text-align: center;
          margin-bottom: 3rem;
          padding-bottom: 2rem;
          border-bottom: 1px solid rgba(0,0,0,0.06);
      }

      .form-title {
          font-size: clamp(1.75rem, 4vw, 2.25rem);
          font-weight: 600;
          color: var(--text-primary);
          margin: 0 0 0.5rem 0;
          letter-spacing: -0.02em;
      }

      .genre-info {
          color: var(--text-secondary);
          font-size: 1rem;
          background: rgba(255,255,255,0.5);
          padding: 0.5rem 1rem;
          border-radius: 12px;
          display: inline-block;
          backdrop-filter: blur(10px);
      }

      .form-grid {
          display: grid;
          gap: 1.75rem;
          margin-bottom: 2.5rem;
      }

      .form-group {
          position: relative;
      }

      .form-label {
          display: block;
          font-weight: 500;
          color: var(--text-primary);
          margin-bottom: 0.75rem;
          font-size: 0.95rem;
          letter-spacing: -0.01em;
      }

      .form-control, .form-select {
          width: 100%;
          padding: 1rem 1.25rem;
          border: 1px solid var(--border-light);
          border-radius: 12px;
          background: rgba(255,255,255,0.9);
          backdrop-filter: blur(10px);
          font-size: 1rem;
          transition: all 0.2s ease;
          font-family: inherit;
          height: 56px;
      }

      .form-control:focus, .form-select:focus {
          outline: none;
          border-color: var(--accent);
          box-shadow: 0 0 0 3px rgba(15,52,96,0.1);
          background: rgba(255,255,255,0.95);
          transform: translateY(-1px);
      }

      .full-width {
          grid-column: 1 / -1;
      }

      .error-alert {
          background: rgba(239,68,68,0.1);
          border: 1px solid rgba(239,68,68,0.2);
          border-radius: 12px;
          padding: 1.5rem;
          margin-bottom: 2rem;
      }

      .error-alert h6 {
          color: #dc2626;
          margin: 0 0 1rem 0;
          font-weight: 600;
          font-size: 0.95rem;
      }

      .error-list {
          list-style: none;
          padding: 0;
          margin: 0;
      }

      .error-list li {
          color: #dc2626;
          padding: 0.25rem 0;
          font-size: 0.9rem;
      }

      .form-actions {
          display: flex;
          gap: 1rem;
          justify-content: center;
          padding: 2rem 0 1rem 0;
          border-top: 1px solid rgba(0,0,0,0.06);
      }

      .btn {
          padding: 1rem 2.5rem;
          border-radius: 12px;
          font-weight: 500;
          font-size: 1rem;
          text-decoration: none;
          border: none;
          cursor: pointer;
          transition: all 0.2s ease;
          display: inline-flex;
          align-items: center;
          gap: 0.5rem;
          backdrop-filter: blur(10px);
          position: relative;
          overflow: hidden;
      }

      .btn-primary {
          background: linear-gradient(135deg, var(--accent), var(--primary-blue));
          color: white;
          box-shadow: 0 8px 25px rgba(15,52,96,0.3);
      }

      .btn-primary:hover {
          transform: translateY(-2px);
          box-shadow: 0 12px 35px rgba(15,52,96,0.4);
      }

      .btn-secondary {
          background: rgba(255,255,255,0.7);
          color: var(--text-secondary);
          border: 1px solid var(--border-light);
      }

      .btn-secondary:hover {
          background: rgba(255,255,255,0.9);
          color: var(--text-primary);
          transform: translateY(-1px);
          box-shadow: 0 5px 20px rgba(0,0,0,0.1);
      }

      @media (max-width: 768px) {
          .form-container {
              padding: 0 1rem;
          }

          .form-card {
              padding: 2.5rem 2rem;
              border-radius: 20px;
              margin: 1rem;
          }

          .form-actions {
              flex-direction: column;
              align-items: center;
          }

          .btn {
              width: 100%;
              max-width: 300px;
              justify-content: center;
          }

          .form-grid {
              gap: 1.25rem;
          }
      }

      @media (max-width: 480px) {
          .form-card {
              padding: 2rem 1.5rem;
              margin: 0.5rem;
          }
      }
  </style>
</head>

<body>
<div class="form-container">
  <div class="form-card">
    <div class="form-header">
      <h1 class="form-title">Novo Livro</h1>
      <% if params[:genero_id].present? %>
        <div class="genre-info">
          Gênero pré-selecionado: <%= Genero.find_by(id: params[:genero_id])&.name %>
        </div>
      <% end %>
    </div>

    <% if @book.errors.any? %>
      <div class="error-alert">
        <h6><%= pluralize(@book.errors.count, "erro") %> encontrado<%= @book.errors.count > 1 ? 's' : '' %>:</h6>
        <ul class="error-list">
          <% @book.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= form_with(model: @book, local: true, class: "form-grid") do |form| %>
      <div class="form-group">
        <%= form.label :name, "Título do Livro", class: "form-label" %>
        <%= form.text_field :name, class: "form-control full-width",
                            placeholder: "Ex: O Nome da Rosa", required: true %>
      </div>

      <div class="form-group">
        <%= form.label :genero_id, "Gênero", class: "form-label" %>
        <%= form.collection_select :genero_id, @generos, :id, :name,
                                   { prompt: "Selecione um gênero" },
                                   { class: "form-select", required: true } %>
      </div>

      <div class="form-group">
        <%= form.label :author_id, "Autor", class: "form-label" %>
        <%= form.collection_select :author_id, @authors, :id, :name,
                                   { prompt: "Selecione um autor" },
                                   { class: "form-select", required: true } %>
      </div>

      <div class="form-group full-width">
        <%= form.label :editora_id, "Editora", class: "form-label" %>
        <%= form.collection_select :editora_id, @editoras, :id, :name,
                                   { prompt: "Selecione uma editora" },
                                   { class: "form-select", required: true } %>
      </div>

      <div class="form-actions">
        <%= form.submit "Criar Livro",
                        class: "btn btn-primary",
                        data: { disable_with: "Criando..." } %>
        <%= link_to "Cancelar", books_path,
                    class: "btn btn-secondary" %>
      </div>
    <% end %>
  </div>
</div>
</body>
</html>
