<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Livros - Biblioteca Mística</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
      :root {
          --primary-dark: #1a1a2e;
          --primary-blue: #16213e;
          --accent: #0f3460;
          --light-bg: rgba(255, 255, 255, 0.92);
          --glass-bg: rgba(255, 255, 255, 0.88);
          --border-light: rgba(0, 0, 0, 0.06);
          --text-primary: #1a202c;
          --text-secondary: #4a5568;
          --text-muted: #a0aec0;
      }

      * {
          box-sizing: border-box;
      }

      body {
          background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary-blue) 50%, var(--accent) 100%);
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
          color: var(--text-primary);
          line-height: 1.6;
          min-height: 100vh;
          padding: 2rem 0;
          margin: 0;
      }

      .books-container {
          max-width: 1600px;
          margin: 0 auto;
          padding: 0 1.5rem;
      }

      .page-header {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 3.5rem;
          text-align: center;
          margin-bottom: 2.5rem;
          box-shadow: 0 25px 70px rgba(0,0,0,0.15);
      }

      .page-title {
          font-size: clamp(2.5rem, 6vw, 3.5rem);
          font-weight: 700;
          color: var(--text-primary);
          margin: 0 0 1rem 0;
          letter-spacing: -0.03em;
      }

      .page-subtitle {
          color: var(--text-secondary);
          font-size: 1.3rem;
          max-width: 600px;
          margin: 0 auto;
      }

      .grid-section-title {
          text-align: center;
          margin-bottom: 2.5rem;
      }

      .grid-section-title h2 {
          font-size: 2.2rem;
          font-weight: 700;
          color: var(--text-primary);
          margin: 0 0 0.75rem 0;
          letter-spacing: -0.02em;
      }

      .grid-section-subtitle {
          color: var(--text-secondary);
          font-size: 1.1rem;
          margin: 0;
          opacity: 0.9;
      }

      .controls-section {
          display: grid;
          grid-template-columns: 1fr auto 1fr;
          gap: 2rem;
          align-items: center;
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 2rem;
          margin-bottom: 3rem;
          box-shadow: 0 20px 60px rgba(0,0,0,0.15);
      }

      .total-books {
          text-align: center;
      }

      .total-books-number {
          font-size: 3.5rem;
          font-weight: 800;
          background: linear-gradient(135deg, var(--accent), var(--primary-blue));
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          background-clip: text;
          line-height: 1;
          margin-bottom: 0.25rem;
      }

      .total-books-label {
          color: var(--text-secondary);
          font-size: 0.95rem;
          font-weight: 500;
          margin: 0;
          opacity: 0.8;
      }

      .sort-tabs {
          display: flex;
          gap: 0.5rem;
          justify-content: center;
          flex-wrap: wrap;
      }

      .sort-tab {
          padding: 0.75rem 1.5rem;
          border: 1px solid rgba(0,0,0,0.08);
          border-radius: 20px;
          background: rgba(255,255,255,0.7);
          color: var(--text-secondary);
          text-decoration: none;
          font-weight: 500;
          font-size: 0.9rem;
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          backdrop-filter: blur(10px);
          white-space: nowrap;
          letter-spacing: -0.02em;
      }

      .sort-tab.active, .sort-tab:hover {
          background: linear-gradient(135deg, var(--accent), var(--primary-blue));
          color: white;
          border-color: var(--accent);
          transform: translateY(-2px);
          box-shadow: 0 10px 25px rgba(15,52,96,0.25);
      }

      .books-grid-section {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 2.5rem 3rem 4rem 3rem;
          box-shadow: 0 25px 70px rgba(0,0,0,0.15);
          margin-bottom: 3rem;
      }

      .books-grid {
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
          gap: 2.5rem;
          list-style: none;
          padding: 0;
          margin: 0;
      }

      .book-card {
          min-height: 420px;
          display: flex;
          flex-direction: column;
          background: rgba(255,255,255,0.95);
          border-radius: 20px;
          overflow: hidden;
          text-decoration: none;
          color: inherit;
          transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
          position: relative;
          border: 1px solid rgba(0,0,0,0.08);
          box-shadow: 0 8px 25px rgba(0,0,0,0.1);
      }

      .book-card::before {
          content: '';
          position: absolute;
          top: 0;
          left: -100%;
          width: 100%;
          height: 100%;
          background: linear-gradient(90deg, transparent, rgba(255,255,255,0.5), transparent);
          transition: left 0.8s;
      }

      .book-card:hover {
          transform: translateY(-16px) scale(1.04);
          box-shadow: 0 35px 80px rgba(0,0,0,0.25);
          border-color: rgba(0,0,0,0.15);
      }

      .book-card:hover::before {
          left: 100%;
      }

      .book-image-placeholder {
          flex: 1;
          width: 100%;
          background: linear-gradient(135deg, #f0f7ff 0%, #e6f3ff 100%);
          border-bottom: 2px solid rgba(15,52,96,0.1);
          display: flex;
          align-items: center;
          justify-content: center;
          position: relative;
      }

      .book-image-placeholder::after {
          content: '\f02d';
          font-family: 'Font Awesome 6 Free';
          font-weight: 900;
          font-size: 3rem;
          opacity: 0.2;
          color: var(--accent);
      }

      .book-info {
          padding: 1.75rem 1.5rem;
          flex-shrink: 0;
          display: flex;
          flex-direction: column;
          justify-content: space-between;
      }

      .book-title {
          font-size: 1.1rem;
          font-weight: 700;
          color: var(--text-primary);
          line-height: 1.35;
          margin: 0 0 1rem 0;
          display: -webkit-box;
          -webkit-line-clamp: 2;
          -webkit-box-orient: vertical;
          overflow: hidden;
      }

      .book-meta {
          font-size: 0.88rem;
          color: var(--text-secondary);
      }

      .book-author {
          font-weight: 600;
          margin-bottom: 0.5rem;
          display: block;
      }

      .book-details {
          font-weight: 500;
          opacity: 0.9;
          font-size: 0.85rem;
      }

      .empty-state {
          min-height: 500px;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          color: var(--text-muted);
          text-align: center;
          padding: 4rem 2rem;
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          box-shadow: 0 20px 60px rgba(0,0,0,0.15);
      }

      .actions-bar {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 3rem;
          display: flex;
          flex-wrap: wrap;
          gap: 1.5rem;
          justify-content: center;
          box-shadow: 0 20px 60px rgba(0,0,0,0.15);
      }

      .action-btn {
          display: inline-flex;
          align-items: center;
          gap: 0.75rem;
          font-weight: 600;
          font-size: 1rem;
          color: var(--text-secondary);
          text-decoration: none;
          padding: 1.25rem 2.5rem;
          border: 2px solid rgba(0,0,0,0.1);
          border-radius: 16px;
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          background: rgba(255,255,255,0.85);
          backdrop-filter: blur(15px);
          white-space: nowrap;
      }

      .action-btn:hover {
          background: rgba(255,255,255,1);
          color: var(--text-primary);
          transform: translateY(-4px);
          box-shadow: 0 20px 45px rgba(0,0,0,0.2);
          border-color: var(--accent);
      }

      @media (max-width: 1200px) {
          .books-grid {
              grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
              gap: 2rem;
          }
      }

      @media (max-width: 992px) {
          .controls-section {
              grid-template-columns: 1fr;
              gap: 2rem;
              text-align: center;
          }

          .books-grid {
              grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
              gap: 2rem;
          }
      }

      @media (max-width: 768px) {
          .books-container {
              padding: 0 1rem;
          }

          .books-grid {
              grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
              gap: 1.5rem;
          }

          .book-card {
              min-height: 360px;
          }

          .book-card:hover {
              transform: translateY(-8px) scale(1.02);
          }

          .page-header, .books-grid-section, .actions-bar {
              padding-left: 2rem;
              padding-right: 2rem;
          }

          .grid-section-title h2 {
              font-size: 1.8rem;
          }
      }

      @media (max-width: 480px) {
          .books-grid {
              grid-template-columns: repeat(auto-fill, minmax(170px, 1fr));
              gap: 1.25rem;
          }

          .book-card {
              min-height: 320px;
          }

          .sort-tab {
              padding: 0.6rem 1.2rem;
              font-size: 0.85rem;
          }
      }
  </style>
</head>
<body>
<div class="books-container">
  <div class="page-header">
    <h1 class="page-title">Biblioteca Completa</h1>
    <p class="page-subtitle">Todos os livros organizados e catalogados da Biblioteca Mística</p>
  </div>

  <div class="controls-section">
    <div class="total-books">
      <div class="total-books-number"><%= @books.count %></div>
      <p class="total-books-label">livros catalogados</p>
    </div>

    <div class="sort-tabs">
      <%= link_to "Mais Recentes", books_path(sort: 'recent'),
                  class: "sort-tab #{'active' if params[:sort] == 'recent'}" %>
      <%= link_to "Mais Antigos", books_path(sort: 'oldest'),
                  class: "sort-tab #{'active' if params[:sort] == 'oldest'}" %>
      <%= link_to "A-Z", books_path(sort: 'alpha'),
                  class: "sort-tab #{'active' if params[:sort] == 'alpha'}" %>
    </div>
  </div>

  <% if @books.any? %>
    <div class="books-grid-section">
      <div class="grid-section-title">
        <h2>Catálogo de Livros</h2>
        <p class="grid-section-subtitle">Explore nossa coleção completa</p>
      </div>
      <ul class="books-grid">
        <% @books.each do |book| %>
          <li>
            <%= link_to book_path(book), class: "book-card" do %>
              <div class="book-image-placeholder"></div>
              <div class="book-info">
                <h3 class="book-title"><%= book.name %></h3>
                <div class="book-meta">
                  <span class="book-author">
                    por <%= book.author&.name || "Autor desconhecido" %>
                  </span>
                  <span class="book-details">
                    <%= book.genero&.name || "Gênero" %> • <%= book.editora&.name || "Editora" %>
                  </span>
                </div>
              </div>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  <% else %>
    <div class="empty-state">
      <i class="fas fa-books" style="font-size: 6rem; color: #cbd5e0; margin-bottom: 2.5rem;"></i>
      <h2>Acervo vazio</h2>
      <p>A biblioteca mística ainda não possui livros. Adicione o primeiro título!</p>
    </div>
  <% end %>

  <div class="actions-bar">
    <%= link_to "<i class='fas fa-arrow-left'></i> Voltar ao inicio!".html_safe, root_path, class: 'action-btn' %>
    <%= link_to "<i class='fas fa-plus-circle'></i> Adicionar Livro".html_safe, new_book_path, class: 'action-btn' %>
    <%= link_to "<i class='fas fa-users'></i> Ver Autores".html_safe, authors_path, class: 'action-btn' %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
