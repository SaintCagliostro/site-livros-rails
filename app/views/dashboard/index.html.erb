<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Biblioteca Mística - Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
      :root {
          --primary-dark: #1a1a2e;
          --primary-blue: #16213e;
          --accent: #0f3460;
          --light-bg: rgba(255, 255, 255, 0.92);
          --glass-bg: rgba(255, 255, 255, 0.88);
          --border-light: rgba(0, 0, 0, 0.06);
          --text-primary: #1a202c;
          --text-secondary: #4a5568;
          --text-muted: #a0aec0;
      }

      * {
          box-sizing: border-box;
      }

      body {
          background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary-blue) 50%, var(--accent) 100%);
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
          color: var(--text-primary);
          line-height: 1.6;
          min-height: 100vh;
          padding: 2rem 0;
          margin: 0;
      }

      .books-container {
          max-width: 1600px;
          margin: 0 auto;
          padding: 0 1.5rem;
      }

      /* Hero Section MINIMALISTA */
      .hero-section {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 3rem 3rem 2rem 3rem;
          text-align: center;
          margin-bottom: 2.5rem;
          box-shadow: 0 25px 70px rgba(0,0,0,0.15);
      }

      /* Barra de pesquisa ACIMA do subtítulo */
      .hero-search {
          max-width: 500px;
          margin: 0 auto 2rem auto;
          position: relative;
      }

      .hero-search-form {
          width: 100%;
      }

      .hero-search-input {
          width: 100%;
          padding: 0.875rem 1rem 0.875rem 2.75rem;
          border: 1px solid rgba(0,0,0,0.08);
          border-radius: 16px;
          font-size: 1rem;
          background: rgba(255,255,255,0.8);
          backdrop-filter: blur(10px);
          color: var(--text-primary);
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          font-weight: 500;
          letter-spacing: -0.01em;
      }

      .hero-search-input:focus {
          outline: none;
          border-color: var(--accent);
          box-shadow: 0 8px 25px rgba(15,52,96,0.15);
          background: rgba(255,255,255,0.95);
      }

      .hero-search-icon {
          position: absolute;
          left: 1.25rem;
          top: 50%;
          transform: translateY(-50%);
          color: var(--text-muted);
          font-size: 1rem;
          pointer-events: none;
      }

      .hero-title {
          font-size: clamp(2.5rem, 6vw, 3.5rem);
          font-weight: 700;
          color: var(--text-primary);
          margin: 0 0 0.5rem 0;
          letter-spacing: -0.03em;
      }

      .hero-subtitle {
          color: var(--text-secondary);
          font-size: 1.3rem;
          font-weight: 500;
          margin: 0;
          opacity: 0.9;
          max-width: 600px;
          margin-left: auto;
          margin-right: auto;
      }

      /* Barra de Navegação EXATA do Código 1 - COM HOVER */
      .nav-bar {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 0;
          margin-bottom: 3rem;
          box-shadow: 0 20px 60px rgba(0,0,0,0.15);
          overflow: visible;
      }

      .nav-grid {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
          gap: 0;
      }

      .nav-item {
          position: relative;
          border-right: 1px solid rgba(0,0,0,0.06);
          overflow: visible;
      }

      .nav-item:last-child {
          border-right: none;
      }

      .nav-label {
          display: flex;
          align-items: center;
          gap: 0.75rem;
          padding: 1.25rem 1.5rem;
          background: rgba(255,255,255,0.6);
          cursor: pointer;
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          border: none;
      }

      .nav-label:hover {
          background: rgba(255,255,255,0.85);
          transform: translateY(-1px);
          box-shadow: 0 8px 20px rgba(0,0,0,0.12);
      }

      .nav-item:hover .nav-label {
          background: rgba(255,255,255,0.9);
      }

      .nav-icon {
          font-size: 1.1rem;
          color: var(--accent);
          width: 28px;
          height: 28px;
          display: flex;
          align-items: center;
          justify-content: center;
          font-weight: 900;
          opacity: 0.8;
      }

      .nav-title {
          font-weight: 500;
          font-size: 1rem;
          color: var(--text-primary);
          flex-grow: 1;
          letter-spacing: -0.01em;
      }

      .nav-count {
          background: linear-gradient(135deg, var(--accent), var(--primary-blue));
          color: white;
          border-radius: 10px;
          padding: 0.375rem 0.875rem;
          font-size: 0.8rem;
          font-weight: 600;
          min-width: 32px;
          text-align: center;
      }

      .dropdown-content {
          background: rgba(255,255,255,0.95);
          border-radius: 0 0 14px 14px;
          border: 1px solid rgba(0,0,0,0.08);
          border-top: none;
          max-height: 0;
          overflow: hidden;
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          backdrop-filter: blur(15px);
          position: absolute;
          left: 0;
          right: 0;
          top: 100%;
          z-index: 1000;
          opacity: 0;
          visibility: hidden;
          transform: translateY(-10px);
      }

      /* HOVER FUNCIONALIDADE - Dropdown aparece no hover */
      .nav-item:hover .dropdown-content {
          max-height: 400px;
          padding: 1.25rem;
          opacity: 1;
          visibility: visible;
          transform: translateY(0);
      }

      .dropdown-item {
          display: block;
          padding: 0.625rem 0.875rem;
          color: var(--text-secondary);
          text-decoration: none;
          border-radius: 10px;
          font-weight: 500;
          font-size: 0.95rem;
          transition: all 0.2s ease;
          margin-bottom: 0.25rem;
          letter-spacing: -0.01em;
      }

      .dropdown-item:hover {
          background: rgba(15,52,96,0.08);
          color: var(--accent);
          padding-left: 1.125rem;
      }

      .more-item {
          font-weight: 600;
          color: var(--accent);
          border-left: 2px solid var(--accent);
          padding-left: 1rem;
      }

      .dropdown-book {
          border: 1px solid rgba(0,0,0,0.06);
          border-radius: 10px;
          padding: 1rem;
          margin-bottom: 0.75rem;
          background: rgba(255,255,255,0.6);
      }

      .book-meta {
          color: var(--text-muted);
          font-size: 0.8rem;
          display: block;
          margin-top: 0.25rem;
      }

      .dropdown-footer {
          margin-top: 1rem;
          padding-top: 1rem;
          border-top: 1px solid rgba(0,0,0,0.06);
      }

      .dropdown-empty {
          padding: 2rem;
          text-align: center;
          color: var(--text-muted);
          font-style: italic;
      }

      .empty-text {
          font-size: 1.1rem;
          margin-bottom: 1rem;
      }

      /* Books Grid Section */
      .books-grid-section {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 4rem 3rem;
          box-shadow: 0 25px 70px rgba(0,0,0,0.15);
          margin-bottom: 3rem;
      }

      .section-header {
          display: flex;
          justify-content: space-between;
          align-items: center;
          margin-bottom: 2.5rem;
      }

      .section-title {
          font-size: 1.8rem;
          font-weight: 700;
          color: var(--text-primary);
          margin: 0;
          letter-spacing: -0.02em;
      }

      .action-btn {
          display: inline-flex;
          align-items: center;
          gap: 0.75rem;
          font-weight: 600;
          font-size: 1rem;
          color: var(--text-secondary);
          text-decoration: none;
          padding: 1rem 2rem;
          border: 2px solid rgba(0,0,0,0.1);
          border-radius: 16px;
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          background: rgba(255,255,255,0.85);
          backdrop-filter: blur(15px);
          white-space: nowrap;
          font-weight: 500;
      }

      .action-btn:hover {
          background: rgba(255,255,255,1);
          color: var(--text-primary);
          transform: translateY(-4px);
          box-shadow: 0 20px 45px rgba(0,0,0,0.2);
          border-color: var(--accent);
      }

      .books-grid {
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
          gap: 2.5rem;
          list-style: none;
          padding: 0;
          margin: 0;
      }

      .book-card {
          aspect-ratio: 3 / 4.2;
          background: rgba(255,255,255,0.95);
          border-radius: 20px;
          overflow: hidden;
          text-decoration: none;
          color: inherit;
          transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
          position: relative;
          border: 1px solid rgba(0,0,0,0.08);
          box-shadow: 0 8px 25px rgba(0,0,0,0.1);
          display: flex;
          flex-direction: column;
      }

      .book-card::before {
          content: '';
          position: absolute;
          top: 0;
          left: -100%;
          width: 100%;
          height: 100%;
          background: linear-gradient(90deg, transparent, rgba(255,255,255,0.5), transparent);
          transition: left 0.8s;
      }

      .book-card:hover {
          transform: translateY(-16px) scale(1.04);
          box-shadow: 0 35px 80px rgba(0,0,0,0.25);
          border-color: rgba(0,0,0,0.15);
      }

      .book-card:hover::before {
          left: 100%;
      }

      .book-image-placeholder {
          width: 100%;
          height: 68%;
          background: linear-gradient(135deg, #f0f7ff 0%, #e6f3ff 100%);
          border-bottom: 2px solid rgba(15,52,96,0.1);
          display: flex;
          align-items: center;
          justify-content: center;
          position: relative;
          flex-shrink: 0;
      }

      .book-image-placeholder::after {
          content: '\f02d';
          font-family: 'Font Awesome 6 Free';
          font-weight: 900;
          font-size: 3rem;
          opacity: 0.2;
          color: var(--accent);
      }

      .book-info {
          padding: 1.75rem 1.5rem;
          height: 32%;
          display: flex;
          flex-direction: column;
          justify-content: space-between;
          flex-grow: 1;
      }

      .book-title {
          font-size: 1.1rem;
          font-weight: 700;
          color: var(--text-primary);
          line-height: 1.35;
          margin: 0 0 1rem 0;
          display: -webkit-box;
          -webkit-line-clamp: 2;
          -webkit-box-orient: vertical;
          overflow: hidden;
      }

      .book-meta {
          font-size: 0.88rem;
          color: var(--text-secondary);
      }

      .book-author {
          font-weight: 600;
          margin-bottom: 0.5rem;
          display: block;
      }

      .book-details {
          font-weight: 500;
          opacity: 0.9;
          font-size: 0.85rem;
      }

      .empty-state {
          min-height: 500px;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          color: var(--text-muted);
          text-align: center;
          padding: 4rem 2rem;
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          box-shadow: 0 20px 60px rgba(0,0,0,0.15);
      }

      /* Responsivo */
      @media (max-width: 1200px) {
          .books-grid {
              grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
              gap: 2rem;
          }
      }

      @media (max-width: 992px) {
          .nav-grid {
              grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
          }
          .books-grid {
              grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
              gap: 2rem;
          }
      }

      @media (max-width: 768px) {
          .books-container {
              padding: 0 1rem;
          }
          .books-grid {
              grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
              gap: 1.5rem;
          }
          .book-card:hover {
              transform: translateY(-8px) scale(1.02);
          }
          .hero-section, .books-grid-section {
              padding-left: 1.5rem;
              padding-right: 1.5rem;
          }
      }

      @media (max-width: 480px) {
          .books-grid {
              grid-template-columns: repeat(auto-fill, minmax(170px, 1fr));
              gap: 1.25rem;
          }
          .nav-grid {
              grid-template-columns: 1fr;
          }
          .nav-item {
              border-right: none;
              border-bottom: 1px solid rgba(0,0,0,0.06);
          }
          .nav-item:last-child {
              border-bottom: none;
          }
      }
  </style>
</head>
<body>
<div class="books-container">
  <!-- Hero Section MINIMALISTA - Pesquisa ACIMA do subtítulo -->
  <section class="hero-section">
    <div class="hero-search">
      <i class="fas fa-search hero-search-icon"></i>
      <%= form_with url: books_path, method: :get, local: true, class: "hero-search-form" do |form| %>
        <%= form.text_field :query,
                            placeholder: "Pesquisar livros...",
                            class: "hero-search-input",
                            autocomplete: "off",
                            value: params[:query] %>
      <% end %>
    </div>

    <h1 class="hero-title">Biblioteca Mística</h1>
    <p class="hero-subtitle">
      Catálogo especializado em literatura esotérica, cabalística e filosófica
    </p>
  </section>

  <!-- Barra de Navegação EXATA do Código 1 - AGORA COM HOVER -->
  <nav class="nav-bar">
    <div class="nav-grid">
      <!-- Gêneros -->
      <div class="nav-item">
        <div class="nav-label">
          <span class="nav-icon">⟐</span>
          <span class="nav-title">Gêneros</span>
          <div class="nav-count"><%= @generos&.count || 0 %></div>
        </div>
        <div class="dropdown-content">
          <% if @generos&.any? %>
            <% @generos.limit(8).each do |genero| %>
              <%= link_to genero.name, genero, class: "dropdown-item" %>
            <% end %>
            <% if @generos.count > 8 %>
              <%= link_to "Ver todos (#{@generos.count})", generos_path, class: "dropdown-item more-item" %>
            <% end %>
          <% else %>
            <div class="dropdown-empty">Nenhum gênero</div>
          <% end %>
        </div>
      </div>

      <!-- Editoras -->
      <div class="nav-item">
        <div class="nav-label">
          <span class="nav-icon">⟐</span>
          <span class="nav-title">Editoras</span>
          <div class="nav-count"><%= @editoras&.count || 0 %></div>
        </div>
        <div class="dropdown-content">
          <% if @editoras&.any? %>
            <% @editoras.limit(8).each do |editora| %>
              <%= link_to editora.name, editora, class: "dropdown-item" %>
            <% end %>
            <% if @editoras.count > 8 %>
              <%= link_to "Ver todas", editoras_path, class: "dropdown-item more-item" %>
            <% end %>
          <% else %>
            <div class="dropdown-empty">Nenhuma editora</div>
          <% end %>
        </div>
      </div>

      <!-- Autores -->
      <div class="nav-item">
        <div class="nav-label">
          <span class="nav-icon">⟐</span>
          <span class="nav-title">Autores</span>
          <div class="nav-count"><%= @authors&.count || 0 %></div>
        </div>
        <div class="dropdown-content">
          <% if @authors&.any? %>
            <% @authors.limit(8).each do |author| %>
              <%= link_to author.name, author, class: "dropdown-item" %>
            <% end %>
            <% if @authors.count > 8 %>
              <%= link_to "Ver todos", authors_path, class: "dropdown-item more-item" %>
            <% end %>
          <% else %>
            <div class="dropdown-empty">Nenhum autor</div>
          <% end %>
        </div>
      </div>

      <!-- Obras -->
      <div class="nav-item">
        <div class="nav-label">
          <span class="nav-icon">⟐</span>
          <span class="nav-title">Obras</span>
          <div class="nav-count"><%= @books&.count || 0 %></div>
        </div>
        <div class="dropdown-content">
          <% if @books&.any? %>
            <% @books.last(6).each do |book| %>
              <div class="dropdown-book">
                <%= link_to book.name, book, class: "dropdown-item" %>
                <small class="book-meta">
                  <%= book.author.name %> • <%= book.genero.name %>
                </small>
              </div>
            <% end %>
            <div class="dropdown-footer">
              <%= link_to "Gerenciar acervo", books_path, class: "action-btn" %>
            </div>
          <% else %>
            <div class="dropdown-empty">
              <div class="empty-text">Acervo vazio</div>
              <%= link_to "Adicionar obra", new_book_path, class: "action-btn" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </nav>

  <!-- Livros Mais Recentes -->
  <% if @books&.any? %>
    <div class="books-grid-section">
      <div class="section-header">
        <h2 class="section-title">Livros Mais Recentes</h2>
        <%= link_to "Ver todos", books_path, class: "action-btn" %>
      </div>
      <ul class="books-grid">
        <% @books.order(created_at: :desc).limit(12).each do |book| %>
          <li>
            <%= link_to book_path(book), class: "book-card" do %>
              <div class="book-image-placeholder"></div>
              <div class="book-info">
                <h3 class="book-title"><%= book.name %></h3>
                <div class="book-meta">
                  <span class="book-author">por <%= book.author.name %></span>
                  <span class="book-details"><%= book.genero.name %> • <%= book.editora.name %></span>
                </div>
              </div>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  <% else %>
    <div class="empty-state">
      <i class="fas fa-books" style="font-size: 6rem; color: #cbd5e0; margin-bottom: 2.5rem;"></i>
      <h2>Acervo vazio</h2>
      <p>A biblioteca mística ainda não possui livros. Adicione o primeiro título!</p>
      <div class="mt-4">
        <%= link_to "<i class='fas fa-plus-circle'></i> Adicionar livro".html_safe, new_book_path, class: "action-btn" %>
      </div>
    </div>
  <% end %>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
