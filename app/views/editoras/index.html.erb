<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editoras - Biblioteca Mística</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
      :root {
          --primary-dark: #1a1a2e;
          --primary-blue: #16213e;
          --accent: #0f3460;
          --light-bg: rgba(255, 255, 255, 0.92);
          --glass-bg: rgba(255, 255, 255, 0.88);
          --border-light: rgba(0, 0, 0, 0.06);
          --text-primary: #1a202c;
          --text-secondary: #4a5568;
          --text-muted: #a0aec0;
      }

      body {
          background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary-blue) 50%, var(--accent) 100%);
          padding: 2rem 0;
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
          color: var(--text-primary);
          line-height: 1.6;
          min-height: 100vh;
          margin: 0;
      }

      .editoras-container {
          max-width: 1200px;
          margin: 0 auto;
          padding: 0 1.5rem;
      }

      .page-header {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 3rem;
          text-align: center;
          margin-bottom: 2.5rem;
          box-shadow: 0 25px 70px rgba(0,0,0,0.15);
      }

      .page-title {
          font-size: clamp(2rem, 5vw, 2.8rem);
          font-weight: 700;
          color: var(--text-primary);
          margin: 0 0 0.5rem 0;
          letter-spacing: -0.02em;
      }

      .page-subtitle {
          color: var(--text-secondary);
          font-size: 1.1rem;
          margin: 0;
      }

      .info-grid {
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
          gap: 2rem;
          margin-top: 2rem;
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 3rem;
          box-shadow: 0 25px 70px rgba(0,0,0,0.15);
      }

      .editora-card {
          background: rgba(255,255,255,0.95);
          backdrop-filter: blur(20px);
          border: 1px solid rgba(0,0,0,0.08);
          border-radius: 20px;
          padding: 2.5rem 2rem;
          height: 100%;
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          text-decoration: none;
          color: inherit;
          display: flex;
          flex-direction: column;
          align-items: center;
          text-align: center;
      }

      .editora-card:hover {
          transform: translateY(-8px) scale(1.02);
          box-shadow: 0 25px 50px rgba(0,0,0,0.2);
      }

      .editora-logo-placeholder {
          width: 90px;
          height: 90px;
          background: linear-gradient(135deg, #f0f7ff 0%, #e6f3ff 100%);
          border-radius: 16px;
          display: flex;
          align-items: center;
          justify-content: center;
          margin: 0 auto 1.5rem;
          font-size: 2.2rem;
          border: 2px solid rgba(15,52,96,0.1);
      }

      .editora-name {
          font-size: 1.3rem;
          font-weight: 700;
          color: var(--text-primary);
          margin-bottom: 0.75rem;
          line-height: 1.3;
          display: -webkit-box;
          -webkit-line-clamp: 2;
          -webkit-box-orient: vertical;
          overflow: hidden;
      }

      .books-count {
          color: var(--text-secondary);
          font-size: 0.95rem;
          font-weight: 500;
      }

      .actions-bar {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 3rem;
          display: flex;
          flex-wrap: wrap;
          gap: 1.5rem;
          justify-content: center;
          box-shadow: 0 20px 60px rgba(0,0,0,0.15);
          margin-top: 2rem;
      }

      .action-btn {
          display: inline-flex;
          align-items: center;
          gap: 0.75rem;
          font-weight: 600;
          font-size: 1rem;
          color: var(--text-secondary);
          text-decoration: none;
          padding: 1.25rem 2.5rem;
          border: 2px solid rgba(0,0,0,0.1);
          border-radius: 16px;
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          background: rgba(255,255,255,0.85);
          backdrop-filter: blur(15px);
          white-space: nowrap;
      }

      .action-btn:hover {
          background: rgba(255,255,255,1);
          color: var(--text-primary);
          transform: translateY(-4px);
          box-shadow: 0 20px 45px rgba(0,0,0,0.2);
          border-color: var(--accent);
      }

      .empty-state {
          min-height: 500px;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          color: var(--text-muted);
          text-align: center;
          padding: 4rem 2rem;
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          box-shadow: 0 20px 60px rgba(0,0,0,0.15);
      }

      @media (max-width: 768px) {
          .editoras-container { padding: 0 1rem; }
          .info-grid { grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1.5rem; padding: 2rem; }
          .editora-card { padding: 2rem 1.5rem; }
      }

      @media (max-width: 480px) {
          .info-grid { grid-template-columns: 1fr; }
          .page-header, .info-grid, .actions-bar { padding-left: 1.5rem; padding-right: 1.5rem; }
      }
  </style>
</head>
<body>
<div class="editoras-container">
  <div class="page-header">
    <h1 class="page-title">
      <i class="fas fa-building me-3" style="color: var(--accent);"></i>
      Editoras
    </h1>
    <p class="page-subtitle">Gerencie todas as editoras da Biblioteca Mística</p>
  </div>

  <% if @editoras.any? %>
    <div class="info-grid">
      <% @editoras.each do |editora| %>
        <div class="editora-card">
          <%= link_to editora_path(editora) do %>
            <div class="editora-logo-placeholder">
              <i class="fas fa-building-columns" style="color: var(--accent); opacity: 0.3;"></i>
            </div>
            <div class="editora-name"><%= editora.name %></div>
            <div class="books-count">
              <i class="fas fa-book me-1"></i>
              <%= editora.books.count %> livro<%= editora.books.count != 1 ? 's' : '' %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="empty-state">
      <i class="fas fa-building-columns" style="font-size: 6rem; color: #cbd5e0; margin-bottom: 2.5rem;"></i>
      <h2>Nenhuma editora cadastrada</h2>
      <p>A biblioteca mística ainda não possui editoras. Cadastre a primeira!</p>
    </div>
  <% end %>

  <div class="actions-bar">
    <%= link_to "<i class='fas fa-arrow-left'></i> Voltar ao inicio!".html_safe, root_path, class: 'action-btn' %>
    <%= link_to "<i class='fas fa-plus-circle'></i> Nova Editora".html_safe, new_editora_path, class: 'action-btn' %>
    <%= link_to "<i class='fas fa-book'></i> Ver Livros".html_safe, books_path, class: 'action-btn' %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
