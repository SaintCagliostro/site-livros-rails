<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editoras - Biblioteca Mística</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
      /* Use o CSS que você já tem + estes ajustes específicos */
      .editoras-container { max-width: 1200px; margin: 0 auto; padding: 2rem 1.5rem; }
      .info-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1.5rem; margin-top: 2rem; }
      .editora-card { background: rgba(255,255,255,0.92); backdrop-filter: blur(20px); border: 1px solid rgba(0,0,0,0.06); border-radius: 16px; padding: 2rem; height: 100%; transition: all 0.3s ease; text-decoration: none; color: inherit; }
      .editora-card:hover { transform: translateY(-4px); box-shadow: 0 20px 40px rgba(0,0,0,0.15); }
      .editora-logo-placeholder { width: 80px; height: 80px; background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%); border-radius: 12px; display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; font-size: 2rem; }
      .editora-name { font-size: 1.25rem; font-weight: 600; color: #1a1a2e; margin-bottom: 0.5rem; }
      .books-count { color: #64748b; font-size: 0.9rem; }
  </style>
</head>
<body style="background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%); padding: 2rem 0; font-family: -apple-system, sans-serif;">
<div class="editoras-container">
  <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem; flex-wrap: wrap; gap: 1rem;">
    <h1 style="font-size: 2.5rem; font-weight: 700; background: linear-gradient(135deg, #ffffff 0%, #e2e8f0 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; margin: 0;">
      <i class="fas fa-building me-3"></i>Editoras
    </h1>
    <%= link_to "<i class='fas fa-plus'></i> Nova Editora".html_safe, new_editora_path,
                class: "btn btn-outline-light border-2 px-4 py-2 fw-semibold", style: "border-radius: 12px; text-decoration: none;" %>
  </div>

  <% if @editoras.any? %>
    <div class="info-grid">
      <% @editoras.each do |editora| %>
        <div class="editora-card">
          <%= link_to editora_path(editora) do %>
            <div class="editora-logo-placeholder">
              <i class="fas fa-building-columns" style="color: #cbd5e0;"></i>
            </div>
            <div class="editora-name"><%= editora.name %></div>
            <div class="books-count">
              <i class="fas fa-book"></i> <%= editora.books.count %> livro<%= editora.books.count != 1 ? 's' : '' %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    <div style="min-height: 400px; display: flex; flex-direction: column; justify-content: center; align-items: center; color: #94a3b8; text-align: center; background: rgba(255,255,255,0.1); backdrop-filter: blur(20px); border-radius: 24px; border: 1px solid rgba(255,255,255,0.2);">
      <i class="fas fa-building-columns" style="font-size: 4rem; margin-bottom: 1rem; opacity: 0.5;"></i>
      <h3>Nenhuma editora cadastrada</h3>
      <p style="max-width: 400px; line-height: 1.6;">Cadastre as primeiras editoras para começar a organizar sua biblioteca mística.</p>
      <%= link_to "<i class='fas fa-plus'></i> Cadastrar Primeira Editora".html_safe, new_editora_path,
                  class: "btn btn-light mt-3 px-4 py-2 fw-semibold", style: "border-radius: 12px;" %>
    </div>
  <% end %>
</div>
</body>
</html>
