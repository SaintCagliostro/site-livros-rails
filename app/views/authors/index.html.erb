<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Autores - Biblioteca Mística</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
      :root {
          --primary-dark: #1a1a2e;
          --primary-blue: #16213e;
          --accent: #0f3460;
          --light-bg: rgba(255, 255, 255, 0.92);
          --glass-bg: rgba(255, 255, 255, 0.88);
          --border-light: rgba(0, 0, 0, 0.06);
          --text-primary: #1a202c;
          --text-secondary: #4a5568;
          --text-muted: #a0aec0;
      }

      * {
          box-sizing: border-box;
      }

      body {
          background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary-blue) 50%, var(--accent) 100%);
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
          color: var(--text-primary);
          line-height: 1.6;
          min-height: 100vh;
          padding: 2rem 0;
          margin: 0;
      }

      .authors-container {
          max-width: 1400px;
          margin: 0 auto;
          padding: 0 1.5rem;
      }

      .page-header {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 3rem;
          text-align: center;
          margin-bottom: 3rem;
          box-shadow: 0 20px 60px rgba(0,0,0,0.15);
      }

      .page-title {
          font-size: clamp(2.25rem, 5vw, 3rem);
          font-weight: 700;
          color: var(--text-primary);
          margin: 0 0 0.5rem 0;
          letter-spacing: -0.02em;
      }

      .page-subtitle {
          color: var(--text-secondary);
          font-size: 1.2rem;
          margin: 0;
      }

      .author-section {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 2.5rem;
          margin-bottom: 2.5rem;
          box-shadow: 0 20px 60px rgba(0,0,0,0.15);
          overflow: hidden;
      }

      .author-bar {
          display: flex;
          align-items: center;
          gap: 1.5rem;
          padding-bottom: 2rem;
          border-bottom: 1px solid rgba(0,0,0,0.06);
          margin-bottom: 2rem;
      }

      .author-avatar {
          width: 80px;
          height: 80px;
          background: linear-gradient(135deg, #f7fafc 0%, #edf2f7 100%);
          border: 2px dashed #e2e8f0;
          border-radius: 12px;
          display: flex;
          align-items: center;
          justify-content: center;
          flex-shrink: 0;
          transition: all 0.3s ease;
      }

      .author-avatar:hover {
          border-color: #cbd5e0;
          transform: scale(1.05);
      }

      .author-info h3 {
          font-size: 1.5rem;
          font-weight: 600;
          color: var(--text-primary);
          margin: 0 0 0.25rem 0;
      }

      .author-book-count {
          color: var(--text-secondary);
          font-size: 0.95rem;
          margin: 0;
      }

      .books-carousel {
          display: flex;
          gap: 1rem;
          overflow-x: auto;
          padding: 1rem 0;
          scroll-behavior: smooth;
          scrollbar-width: none;
          -ms-overflow-style: none;
      }

      .books-carousel::-webkit-scrollbar {
          display: none;
      }

      .books-carousel .book-card {
          flex: 0 0 180px;
          aspect-ratio: 2.8 / 4;
      }

      .book-card {
          background: rgba(255,255,255,0.7);
          border-radius: 12px;
          overflow: hidden;
          text-decoration: none;
          color: inherit;
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          position: relative;
          border: 1px solid rgba(0,0,0,0.05);
      }

      .book-card::before {
          content: '';
          position: absolute;
          top: 0;
          left: -100%;
          width: 100%;
          height: 100%;
          background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
          transition: left 0.6s;
      }

      .book-card:hover {
          transform: translateY(-6px) scale(1.02);
          box-shadow: 0 15px 35px rgba(0,0,0,0.15);
          border-color: rgba(0,0,0,0.1);
      }

      .book-card:hover::before {
          left: 100%;
      }

      .book-image-placeholder {
          width: 100%;
          height: 70%;
          background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
          border-bottom: 1px solid rgba(0,0,0,0.05);
          display: flex;
          align-items: center;
          justify-content: center;
          position: relative;
      }

      .book-image-placeholder::after {
          content: '📖';
          font-size: 2rem;
          opacity: 0.3;
      }

      .book-title {
          padding: 0.875rem 0.75rem 0.625rem;
          font-size: 0.85rem;
          font-weight: 600;
          color: var(--text-primary);
          line-height: 1.3;
          display: -webkit-box;
          -webkit-line-clamp: 3;
          -webkit-box-orient: vertical;
          overflow: hidden;
      }

      .empty-state {
          min-height: 400px;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          color: var(--text-muted);
          text-align: center;
          padding: 2rem;
      }

      .actions-bar {
          display: flex;
          flex-wrap: wrap;
          gap: 0.75rem;
          justify-content: center;
          padding: 2rem 0;
          border-top: 1px solid rgba(0,0,0,0.06);
      }

      .action-btn {
          display: inline-flex;
          align-items: center;
          gap: 0.5rem;
          font-weight: 500;
          font-size: 0.9rem;
          color: var(--text-secondary);
          text-decoration: none;
          padding: 0.5rem 1.25rem;
          border: 1px solid rgba(0,0,0,0.1);
          border-radius: 8px;
          transition: all 0.2s ease;
          background: rgba(255,255,255,0.6);
          backdrop-filter: blur(10px);
      }

      .action-btn:hover {
          background: rgba(255,255,255,0.9);
          color: var(--text-primary);
          transform: translateY(-1px);
          box-shadow: 0 5px 15px rgba(0,0,0,0.1);
      }

      @media (max-width: 992px) {
          .authors-container {
              padding: 0 1rem;
          }

          .author-bar {
              flex-direction: column;
              text-align: center;
              gap: 1rem;
          }

          .author-avatar {
              width: 100px;
              height: 100px;
          }
      }

      @media (max-width: 768px) {
          .page-header {
              padding: 2rem 1.5rem;
          }

          .author-section {
              padding: 2rem 1.5rem;
          }

          .books-carousel {
              gap: 0.75rem;
          }

          .books-carousel .book-card {
              flex: 0 0 150px;
          }

          .actions-bar {
              flex-direction: column;
              align-items: center;
          }

          .action-btn {
              width: 100%;
              max-width: 280px;
              justify-content: center;
          }
      }
  </style>
</head>
<body>
<div class="authors-container">
  <div class="page-header">
    <h1 class="page-title">Autores</h1>
    <p class="page-subtitle">Todos os autores da Biblioteca Mística e suas principais obras</p>
  </div>

  <% authors_with_books = @authors.select { |author| author.books.any? }.sort_by { |author| author.books.first.created_at } %>

  <% if authors_with_books.any? %>
    <% authors_with_books.each do |author| %>
      <div class="author-section">
        <div class="author-bar">
          <%= link_to author_path(author) do %>
            <div class="author-avatar">
              <i class="fas fa-user" style="font-size: 1.8rem; color: #cbd5e0;"></i>
            </div>
          <% end %>
          <div class="author-info">
            <h3><%= link_to author.name, author_path(author) %></h3>
            <p class="author-book-count"><%= author.books.count %> livro<%= author.books.count > 1 ? 's' : '' %></p>
          </div>
        </div>

        <div class="books-carousel">
          <% author.books.order(created_at: :asc).each do |book| %>
            <%= link_to book_path(book), class: "book-card" do %>
              <div class="book-image-placeholder"></div>
              <div class="book-title"><%= book.name %></div>
            <% end %>
          <% end %>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="empty-state">
      <i class="fas fa-users" style="font-size: 4rem; color: #cbd5e0; margin-bottom: 1.5rem;"></i>
      <h3>Nenhum autor com livros encontrado</h3>
      <p>Adicione autores e livros para começar a organizar sua biblioteca.</p>
    </div>
  <% end %>

  <div class="actions-bar">
    <%= link_to "<i class='fas fa-arrow-left'></i> Pagina Inicial".html_safe, root_path, class: 'action-btn' %>
    <%= link_to "<i class='fas fa-user-plus'></i> Novo Autor".html_safe, new_author_path, class: 'action-btn' %>
    <%= link_to "<i class='fas fa-book'></i> Todos os Livros".html_safe, books_path, class: 'action-btn' %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
