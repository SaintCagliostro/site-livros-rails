<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Novo Autor - Biblioteca Mística</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
      :root {
          --primary-dark: #1a1a2e;
          --primary-blue: #16213e;
          --accent: #0f3460;
          --light-bg: rgba(255, 255, 255, 0.92);
          --glass-bg: rgba(255, 255, 255, 0.88);
          --border-light: rgba(0, 0, 0, 0.06);
          --text-primary: #1a202c;
          --text-secondary: #4a5568;
          --text-muted: #a0aec0;
      }

      body {
          background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary-blue) 50%, var(--accent) 100%);
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
          color: var(--text-primary);
          line-height: 1.6;
          min-height: 100vh;
          padding: 2rem 0;
          margin: 0;
      }

      .form-container {
          max-width: 900px;
          margin: 0 auto;
          padding: 0 1.5rem;
      }

      .form-section {
          background: var(--light-bg);
          backdrop-filter: blur(20px);
          border: 1px solid var(--border-light);
          border-radius: 24px;
          padding: 3rem;
          box-shadow: 0 25px 70px rgba(0,0,0,0.15);
          margin-bottom: 2rem;
      }

      .section-header {
          display: flex;
          align-items: center;
          gap: 0.75rem;
          margin-bottom: 2.5rem;
          padding-bottom: 1.5rem;
          border-bottom: 2px solid rgba(0,0,0,0.06);
      }

      .section-title {
          font-size: 1.6rem;
          font-weight: 600;
          color: var(--text-primary);
          margin: 0;
          letter-spacing: -0.02em;
      }

      .section-icon {
          font-size: 1.3rem;
          color: var(--accent);
          width: 28px;
          height: 28px;
          display: flex;
          align-items: center;
          justify-content: center;
      }

      .count-badge {
          background: linear-gradient(135deg, var(--accent), var(--primary-blue));
          color: white;
          border-radius: 12px;
          padding: 0.25rem 0.75rem;
          font-size: 0.8rem;
          font-weight: 600;
          margin-left: 1rem;
      }

      .form-floating {
          margin-bottom: 1.75rem;
      }

      .form-floating > .form-control,
      .form-floating > .form-select {
          border: 1px solid rgba(0,0,0,0.08);
          border-radius: 16px;
          padding: 1rem 1.25rem;
          font-size: 1rem;
          background: rgba(255,255,255,0.8);
          backdrop-filter: blur(10px);
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          height: calc(3.5rem + 2px);
          border-bottom-left-radius: 0;
          border-bottom-right-radius: 0;
      }

      .form-floating > .form-control:focus,
      .form-floating > .form-select:focus {
          border-color: var(--accent);
          box-shadow: 0 8px 25px rgba(15,52,96,0.15);
          background: rgba(255,255,255,0.95);
          border-bottom-color: rgba(0,0,0,0.08);
      }

      .form-floating > label {
          padding: 1rem 1.25rem;
          font-weight: 500;
          color: var(--text-secondary);
          font-size: 0.95rem;
      }

      .form-floating > .form-control:focus ~ label,
      .form-floating > .form-control:not(:placeholder-shown) ~ label,
      .form-floating > .form-select ~ label {
          color: var(--accent);
          font-weight: 500;
      }

      .form-floating > .form-control.is-invalid {
          border-color: #dc3545;
      }

      .invalid-feedback {
          font-size: 0.875rem;
          color: #dc3545;
          margin-top: 0.25rem;
          display: block;
      }

      .nested-form {
          background: rgba(255,255,255,0.6);
          border: 1px solid rgba(0,0,0,0.06);
          border-radius: 16px;
          padding: 2rem;
          margin-bottom: 1.75rem;
          transition: all 0.3s ease;
          position: relative;
      }

      .nested-form:hover {
          background: rgba(255,255,255,0.75);
          box-shadow: 0 8px 25px rgba(0,0,0,0.1);
          transform: translateY(-2px);
      }

      .nested-form-header {
          display: flex;
          justify-content: between;
          align-items: center;
          margin-bottom: 1.75rem;
          padding-bottom: 1rem;
          border-bottom: 1px solid rgba(0,0,0,0.06);
      }

      .nested-title {
          font-size: 1.1rem;
          font-weight: 600;
          color: var(--text-primary);
          margin: 0;
      }

      .form-row {
          display: grid;
          grid-template-columns: 1fr 1fr 1fr;
          gap: 1.5rem;
          margin-bottom: 1.75rem;
      }

      .form-group {
          margin-bottom: 0;
      }

      .form-row-end {
          display: flex;
          justify-content: flex-end;
          align-items: center;
          gap: 1rem;
          margin-top: 1.5rem;
      }

      .form-check-input:checked {
          background-color: var(--accent);
          border-color: var(--accent);
      }

      .form-check-label {
          font-weight: 500;
          color: var(--text-secondary);
      }

      .btn-action {
          display: inline-flex;
          align-items: center;
          gap: 0.5rem;
          font-weight: 500;
          padding: 0.75rem 1.75rem;
          border-radius: 12px;
          text-decoration: none;
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          font-size: 0.95rem;
          border: 2px solid transparent;
      }

      .btn-primary-custom {
          background: linear-gradient(135deg, var(--accent), var(--primary-blue));
          color: white;
          border-color: var(--accent);
          padding: 1rem 2.5rem;
          font-size: 1.05rem;
          font-weight: 600;
      }

      .btn-primary-custom:hover {
          background: linear-gradient(135deg, var(--primary-blue), var(--accent));
          color: white;
          transform: translateY(-2px);
          box-shadow: 0 12px 35px rgba(15,52,96,0.4);
      }

      .btn-secondary-custom {
          background: rgba(255,255,255,0.9);
          color: var(--text-secondary);
          border-color: rgba(0,0,0,0.1);
      }

      .btn-secondary-custom:hover {
          background: white;
          color: var(--text-primary);
          transform: translateY(-1px);
          box-shadow: 0 8px 25px rgba(0,0,0,0.12);
      }

      .btn-danger-custom {
          background: rgba(220, 53, 69, 0.1);
          color: #dc3545;
          border-color: rgba(220, 53, 69, 0.2);
      }

      .btn-danger-custom:hover {
          background: rgba(220, 53, 69, 0.15);
          color: #dc3545;
          transform: translateY(-1px);
      }

      .btn-add {
          background: rgba(255,255,255,0.9);
          color: var(--accent);
          border: 2px dashed rgba(15,52,96,0.3);
          padding: 1rem 2rem;
          font-weight: 500;
          border-radius: 16px;
          transition: all 0.3s ease;
      }

      .btn-add:hover {
          background: rgba(255,255,255,1);
          border-color: var(--accent);
          transform: translateY(-2px);
          box-shadow: 0 12px 35px rgba(15,52,96,0.2);
      }

      .actions-row {
          display: flex;
          gap: 1rem;
          justify-content: flex-end;
          margin-top: 3rem;
          padding-top: 2rem;
          border-top: 2px solid rgba(0,0,0,0.06);
      }

      @media (max-width: 992px) {
          .form-row {
              grid-template-columns: 1fr;
              gap: 1.25rem;
          }

          .form-section {
              padding: 2rem 1.5rem;
          }
      }

      @media (max-width: 768px) {
          .form-container {
              padding: 0 1rem;
          }

          .actions-row {
              flex-direction: column;
          }

          .btn-primary-custom {
              width: 100%;
          }
      }
  </style>
</head>
<body>
<div class="form-container">
  <%= form_with model: @author, local: true do |f| %>
    <div class="form-section">
      <!-- Informações do Autor -->
      <div class="section-header">
        <i class="fas fa-user-edit section-icon"></i>
        <h2 class="section-title">Informações do Autor</h2>
      </div>

      <div class="form-floating">
        <%= f.label :name, "Nome do Autor", class: "floating-label" %>
        <%= f.text_field :name,
                         class: "form-control #{ 'is-invalid' if @author.errors[:name].any? }",
                         placeholder: "Digite o nome do autor",
                         required: true %>
        <% if @author.errors[:name].any? %>
          <div class="invalid-feedback d-block">
            <%= @author.errors[:name].first %>
          </div>
        <% end %>
      </div>

      <div class="form-floating">
        <%= f.label :bio, "Biografia", class: "floating-label" %>
        <%= f.text_area :bio,
                        class: "form-control #{ 'is-invalid' if @author.errors[:bio].any? }",
                        rows: 5,
                        placeholder: "Breve biografia do autor",
                        required: true %>
        <% if @author.errors[:bio].any? %>
          <div class="invalid-feedback d-block">
            <%= @author.errors[:bio].first %>
          </div>
        <% end %>
      </div>

      <!-- Livros (Nested Forms) -->
      <div id="books-container">
        <div class="section-header">
          <i class="fas fa-book section-icon"></i>
          <h2 class="section-title">
            Livros do Autor
            <span class="count-badge"><%= @author.books.size %></span>
          </h2>
        </div>

        <%= f.fields_for :books do |book_form| %>
          <div class="nested-form" id="<%= dom_id(book_form.object) %>">
            <div class="nested-form-header">
              <h3 class="nested-title">Novo Livro</h3>
            </div>

            <div class="form-row">
              <div class="form-group form-floating">
                <%= book_form.label :name, "Nome do Livro", class: "floating-label" %>
                <%= book_form.text_field :name,
                                         class: "form-control #{ 'is-invalid' if book_form.object.errors[:name].any? }",
                                         placeholder: "Título do livro",
                                         required: true %>
                <% if book_form.object.errors[:name].any? %>
                  <div class="invalid-feedback d-block">
                    <%= book_form.object.errors[:name].first %>
                  </div>
                <% end %>
              </div>

              <div class="form-group form-floating">
                <%= book_form.label :genero_id, "Gênero", class: "floating-label" %>
                <%= book_form.collection_select :genero_id,
                                                Genero.all,
                                                :id,
                                                :name,
                                                { prompt: "Selecione o gênero" },
                                                class: "form-select #{ 'is-invalid' if book_form.object.errors[:genero_id].any? }",
                                                required: true %>
              </div>

              <div class="form-group form-floating">
                <%= book_form.label :editora_id, "Editora", class: "floating-label" %>
                <%= book_form.collection_select :editora_id,
                                                Editora.all,
                                                :id,
                                                :name,
                                                { prompt: "Selecione a editora" },
                                                class: "form-select #{ 'is-invalid' if book_form.object.errors[:editora_id].any? }",
                                                required: true %>
              </div>
            </div>

            <div class="form-row-end">
              <%= book_form.check_box :_destroy, class: "form-check-input me-2" %>
              <%= book_form.label :_destroy, "Remover este livro", class: "form-check-label" %>

              <% unless book_form.object.persisted? %>
                <button type="button" class="btn btn-danger-custom btn-action" onclick="removeBook(this)">
                  <i class="fas fa-trash"></i> Remover
                </button>
              <% end %>
            </div>
          </div>
        <% end %>

        <!-- Botão para adicionar mais livros -->
        <div class="text-center mt-4">
          <button type="button" class="btn btn-add btn-action" onclick="addBook()">
            <i class="fas fa-plus-circle me-2"></i>
            Adicionar Livro
          </button>
        </div>
      </div>

      <!-- Botões de ação -->
      <div class="actions-row">
        <%= link_to "Cancelar", authors_path, class: "btn btn-secondary-custom btn-action" %>
        <%= f.submit "Salvar Autor", class: "btn btn-primary-custom btn-action" %>
      </div>
    </div>
  <% end %>
</div>

<script>
    let bookIndex = <%= @author.books.size %>;

    function addBook() {
        const container = document.getElementById("books-container");
        const bookTemplate = `
        <div class="nested-form" id="book_${bookIndex}">
          <div class="nested-form-header">
            <h3 class="nested-title">Novo Livro</h3>
          </div>

          <div class="form-row">
            <div class="form-group form-floating">
              <label class="floating-label">Nome do Livro</label>
              <input type="text"
                     name="author[books_attributes][${bookIndex}][name]"
                     id="author_books_attributes_${bookIndex}_name"
                     class="form-control"
                     placeholder="Título do livro"
                     required>
            </div>
            <div class="form-group form-floating">
              <label class="floating-label">Gênero</label>
              <select name="author[books_attributes][${bookIndex}][genero_id]"
                      id="author_books_attributes_${bookIndex}_genero_id"
                      class="form-select" required>
                <option value="">Selecione o gênero</option>
                <%= options_from_collection_for_select(Genero.all, :id, :name) %>
              </select>
            </div>
            <div class="form-group form-floating">
              <label class="floating-label">Editora</label>
              <select name="author[books_attributes][${bookIndex}][editora_id]"
                      id="author_books_attributes_${bookIndex}_editora_id"
                      class="form-select" required>
                <option value="">Selecione a editora</option>
                <%= options_from_collection_for_select(Editora.all, :id, :name) %>
              </select>
            </div>
          </div>

          <div class="form-row-end">
            <label class="form-check-label">
              <input type="checkbox"
                     name="author[books_attributes][${bookIndex}][_destroy]"
                     id="author_books_attributes_${bookIndex}_destroy"
                     class="form-check-input me-2">
              Remover este livro
            </label>
            <button type="button" class="btn btn-danger-custom btn-action" onclick="removeBook(this)">
              <i class="fas fa-trash"></i> Remover
            </button>
          </div>
        </div>
      `;

        const addButtonContainer = container.querySelector('.text-center');
        addButtonContainer.insertAdjacentHTML('beforebegin', bookTemplate);
        bookIndex++;
    }

    function removeBook(button) {
        const bookCard = button.closest('.nested-form');
        bookCard.style.display = 'none';
        const destroyCheckbox = bookCard.querySelector('input[type="checkbox"][name*="_destroy"]');
        if (destroyCheckbox) {
            destroyCheckbox.checked = true;
        }
    }
</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
